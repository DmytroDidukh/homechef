export enum APP_REQUEST_STATUS_ENUM {
    IDLE = 'IDLE',
    PENDING = 'PENDING',
    ERROR = 'ERROR',
    SUCCESS = 'SUCCESS',
}

export enum SIGN_IN_ENUM {
    GOOGLE = 'google',
    FACEBOOK = 'facebook',
}
