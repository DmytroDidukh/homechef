export enum SPINNER_SIZE_ENUM {
    SMALL = 'small',
    MEDIUM = 'medium',
    LARGE = 'large',
}

export enum SPINNER_STYLE_ENUM {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    LIGHT = 'light',
    DARK = 'dark',
}
