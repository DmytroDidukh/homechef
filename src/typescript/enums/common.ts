export enum LOADER_SIZE_ENUM {
    SMALL = 'small',
    MEDIUM = 'medium',
    LARGE = 'large',
}

export enum LOADER_STYLE_ENUM {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    LIGHT = 'light',
    DARK = 'dark',
}
